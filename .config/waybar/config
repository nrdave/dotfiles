{
	"include": "$XDG_CONFIG_HOME/waybar/hardware_config",

    "layer": "top",
    "position": "bottom",
    "spacing": 2,

    "modules-left": ["custom/launcher", "sway/mode", "sway/scratchpad"],
	"modules-center": ["sway/workspaces"],
    "modules-right": ["temperature", "group/host-specific", "wireplumber", "tray", "clock"],

    
	"sway/mode": {
        "format": "<span style=\"italic\">{}</span>"
    },
    "sway/scratchpad": {
        "format": "{icon} {count}",
        "show-empty": false,
        "format-icons": ["", ""],
        "tooltip": true,
        "tooltip-format": "{app}: {title}"
    },
   "tray": {
		"icon-size": 24,
    },
    "clock": {
        "tooltip-format": "<big>{:%Y %B}</big>\n<tt><small>{calendar}</small></tt>",
		"format": "{:%A, %B %d, %I:%M:%S %p %Z}",
        "format-alt": "{:%Y-%m-%dT%H:%M:%S%z}",
		"interval": 1,
    },
    "temperature": {
        "critical-threshold": 80,
        "format": "{temperatureC}°C {icon}",
        "format-icons": ["", "", ""],
		"interval": 5,
    },
    "wireplumber": {
        "format": "{icon}",
        "format-icons": ["", "", "", ""],
		"format-muted": "",
        "on-click": "pavucontrol"
    },
	"custom/launcher": {
        "format": "<span font='18'>{icon}</span>",
        "format-icons": [""],
		"on-click": "swaymsg exec fuzzel",
		"tooltip": false,
	}
}

